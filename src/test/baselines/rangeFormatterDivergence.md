# Range formatter divergence baseline

Generated by `src/test/rangeDifferential.test.ts`. Do not edit by hand - run
`pnpm exec vitest run -u` and review the diff.

28 selections: 13 identical, 10 differ, 5 the engine cannot be asked about.

Not idempotent under the range formatter - `format(format(x))` differs from `format(x)`: 1.

## balanced subtree

**identical**

Selection:

```xml
<Root><Child attr="1"><Leaf/></Child></Root>
```

Range formatter today:

```xml
<Root>
    <Child attr="1">
        <Leaf />
    </Child>
</Root>
```

Engine, through a synthetic root:

```xml
<Root>
    <Child attr="1">
        <Leaf />
    </Child>
</Root>
```

## balanced forest

**differs**

Selection:

```xml
<a><b>1</b></a><a2/>
```

Range formatter today:

```xml
<a>
    <b>
        1
    </b>
</a>
<a2 />
```

Engine, through a synthetic root:

```xml
<a>
    <b>1</b>
</a>
<a2 />
```

## attributes over the newline threshold

**identical**

Selection:

```xml
<a x="1" y="2" z="3"/>
```

Range formatter today:

```xml
<a x="1"
   y="2"
   z="3" />
```

Engine, through a synthetic root:

```xml
<a x="1"
   y="2"
   z="3" />
```

## single attribute

**identical**

Selection:

```xml
<a x="1"/>
```

Range formatter today:

```xml
<a x="1" />
```

Engine, through a synthetic root:

```xml
<a x="1" />
```

## empty element written long

**differs**

Selection:

```xml
<a></a>
```

Range formatter today:

```xml
<a>
</a>
```

Engine, through a synthetic root:

```xml
<a />
```

## comment only

**identical**

Selection:

```xml
<!--   hello   -->
```

Range formatter today:

```xml
<!-- hello -->
```

Engine, through a synthetic root:

```xml
<!-- hello -->
```

## cdata only

**identical**

Selection:

```xml
<![CDATA[raw <stuff> here]]>
```

Range formatter today:

```xml
<![CDATA[raw <stuff> here]]>
```

Engine, through a synthetic root:

```xml
<![CDATA[raw <stuff> here]]>
```

## text only

**identical**

Selection:

```xml
just text
```

Range formatter today:

```xml
just text
```

Engine, through a synthetic root:

```xml
just text
```

## mixed content

**differs**

Selection:

```xml
<p>some <b>bold</b> text</p>
```

Range formatter today:

```xml
<p>
    some
    <b>
        bold
    </b>
    text
</p>
```

Engine, through a synthetic root:

```xml
<p>some <b>bold</b> text</p>
```

## nested three deep

**differs**

Selection:

```xml
<a><b><c>deep</c></b></a>
```

Range formatter today:

```xml
<a>
    <b>
        <c>
            deep
        </c>
    </b>
</a>
```

Engine, through a synthetic root:

```xml
<a>
    <b>
        <c>deep</c>
    </b>
</a>
```

## blank line between siblings

**identical**

Selection:

```xml
<a/>

<b/>
```

Range formatter today:

```xml
<a />
<b />
```

Engine, through a synthetic root:

```xml
<a />
<b />
```

## already formatted, four space indent

**differs**

Selection:

```xml
<a>
    <b>t</b>
</a>
```

Range formatter today:

```xml
<a>
    <b>
        t
    </b>
</a>
```

Engine, through a synthetic root:

```xml
<a>
    <b>t</b>
</a>
```

## greater-than inside an attribute value

**differs**

Selection:

```xml
<a b="x>y"><c/></a>
```

Range formatter today:

```xml
<a b
   "x>
    y">
    <c />
</a>
```

Engine, through a synthetic root:

```xml
<a b="x&gt;y">
    <c />
</a>
```

## self-closing slash with trailing spaces

**engine unreachable - the tree formatter is on its own here** - and formatting it twice does not settle

Selection:

```xml
<r><a /  ><b/></r>
```

Range formatter today:

```xml
<r>
    <a />
        <b />
    </r>
```

Engine, through a synthetic root:

```xml
[engine rejects] ' ' is an unexpected token. The expected token is '>'.
```

## single-quoted attribute value

**identical**

Selection:

```xml
<a x='1' y='2'/>
```

Range formatter today:

```xml
<a x="1"
   y="2" />
```

Engine, through a synthetic root:

```xml
<a x="1"
   y="2" />
```

## apostrophe inside a double-quoted value

**identical**

Selection:

```xml
<a x="it's"/>
```

Range formatter today:

```xml
<a x="it's" />
```

Engine, through a synthetic root:

```xml
<a x="it's" />
```

## namespace prefix bound outside the selection

**differs**

Selection:

```xml
<xsl:template match="/"><xsl:text>x</xsl:text></xsl:template>
```

Range formatter today:

```xml
<xsl:template match="/">
    <xsl:text>
        x
    </xsl:text>
</xsl:template>
```

Engine, through a synthetic root:

```xml
<xsl:template match="/">
    <xsl:text>x</xsl:text>
</xsl:template>
```

## namespace prefix declared inside the selection

**identical**

Selection:

```xml
<a xmlns:p="urn:x"><p:b/></a>
```

Range formatter today:

```xml
<a xmlns:p="urn:x">
    <p:b />
</a>
```

Engine, through a synthetic root:

```xml
<a xmlns:p="urn:x">
    <p:b />
</a>
```

## several namespace prefixes bound outside the selection

**identical**

Selection:

```xml
<d:Grid x:Name="root" m:Ignorable="d"><d:Text/></d:Grid>
```

Range formatter today:

```xml
<d:Grid x:Name="root"
        m:Ignorable="d">
    <d:Text />
</d:Grid>
```

Engine, through a synthetic root:

```xml
<d:Grid x:Name="root"
        m:Ignorable="d">
    <d:Text />
</d:Grid>
```

## undeclared entity

**engine unreachable - the tree formatter is on its own here**

Selection:

```xml
<a>&myEntity;</a>
```

Range formatter today:

```xml
<a>
    &myEntity;
</a>
```

Engine, through a synthetic root:

```xml
[engine rejects] Reference to undeclared entity 'myEntity'.
```

## declaration at the start of the selection

**identical**

Selection:

```xml
<?xml version="1.0"?><a/>
```

Range formatter today:

```xml
<?xml version="1.0"?>
<a />
```

Engine, through a synthetic root:

```xml
<?xml version="1.0"?>
<a />
```

## doctype at the start of the selection

**identical**

Selection:

```xml
<!DOCTYPE a><a/>
```

Range formatter today:

```xml
<!DOCTYPE a>
<a />
```

Engine, through a synthetic root:

```xml
<!DOCTYPE a>
<a />
```

## processing instruction

**differs**

Selection:

```xml
<?pi data?><a/>
```

Range formatter today:

```xml
<?pi data?>
<a />
```

Engine, through a synthetic root:

```xml
<?pi data?>

<a />
```

## xml:space preserve inside the selection

**differs**

Selection:

```xml
<a xml:space="preserve">  keep   me  </a>
```

Range formatter today:

```xml
<a xml:space="preserve">
    keep   me
</a>
```

Engine, through a synthetic root:

```xml
<a xml:space="preserve">  keep   me  </a>
```

## orphan end tags

**engine unreachable - the tree formatter is on its own here**

Selection:

```xml
</b></a>
```

Range formatter today:

```xml
</b>
</a>
```

Engine, through a synthetic root:

```xml
[not delegable] unbalanced: </b> closes nothing in the selection
```

## unclosed start tags

**engine unreachable - the tree formatter is on its own here**

Selection:

```xml
<a><b>text
```

Range formatter today:

```xml
<a>
    <b>
        text
```

Engine, through a synthetic root:

```xml
[not delegable] unbalanced: <a> is never closed in the selection
```

## selection ends mid-tag

**engine unreachable - the tree formatter is on its own here**

Selection:

```xml
<a><b attr="v
```

Range formatter today:

```xml
<a>
    <b attr="v
```

Engine, through a synthetic root:

```xml
[not delegable] selection ends mid-token
```

## leading indentation on the first line

**differs**

Selection:

```xml
        <a><b/></a>
```

Range formatter today:

```xml
        <a>
            <b />
        </a>
```

Engine, through a synthetic root:

```xml
<a>
    <b />
</a>
```
